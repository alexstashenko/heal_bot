# 🐙 Инструкция по созданию GitHub репозитория

## Шаг 1: Войти в GitHub ✅

1. Войдите в свой GitHub аккаунт на странице https://github.com/login
2. После входа перейдите на https://github.com/new

## Шаг 2: Заполнить форму создания репозитория

### Основные настройки:
- **Repository name**: `heal_bot`
- **Description** (опционально): `HEAL Wellness Telegram Bot with Gemini Flash AI`
- **Visibility**: `Private` (рекомендую для начала) или `Public`

### НЕ выбирайте:
- ❌ **Add a README file** - у нас уже есть
- ❌ **Add .gitignore** - у нас уже есть
- ❌ **Choose a license** - можно добавить позже

### Нажмите: `Create repository`

## Шаг 3: Подключить локальный репозиторий

После создания GitHub покажет команды. Используйте раздел **"…or push an existing repository from the command line"**

У вас будут команды вида:
```bash
git remote add origin https://github.com/YOUR_USERNAME/heal_bot.git
git branch -M main
git push -u origin main
```

## Шаг 4: Выполнить команды

**Я уже подготовил репозиторий локально:**
- ✅ `git init` - выполнено
- ✅ `git add .` - выполнено
- ✅ `git commit` - выполнено (13 файлов)

**Вам нужно выполнить только:**

1. Скопировать URL вашего нового репозитория (он будет на странице GitHub)
2. Выполнить в терминале:

```bash
cd /Users/user/Desktop/heal_bot

# Замените YOUR_USERNAME на ваш username
git remote add origin https://github.com/YOUR_USERNAME/heal_bot.git

git branch -M main

git push -u origin main
```

## Когда скопируете URL с GitHub - дайте знать!

Я помогу выполнить команды подключения и push.

---

## Что будет загружено в репозиторий:

✅ Код бота (`src/bot.js`, `src/services/gemini.js`)  
✅ Конфигурация (`package.json`, `.gitignore`)  
✅ Документация (`README.md`, `LAUNCH_TODAY.md`)  
✅ Планы и материалы (все .md файлы)

❌ **НЕ будет загружено** (защищено .gitignore):
- `node_modules/` - зависимости
- `.env` - ваши секретные ключи
- Логи и временные файлы
