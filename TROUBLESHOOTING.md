# 🔧 Диагностика ошибки Gemini API

## Что исправлено

✅ Изменена модель с `gemini-2.0-flash-exp` (экспериментальная) на `gemini-1.5-flash` (стабильная)
✅ Добавлено детальное логирование ошибок

## Шаги для устранения ошибки

### 1. Проверьте API ключ Gemini

Откройте файл `.env` и убедитесь что:
- Файл существует в `/Users/user/Desktop/heal_bot/.env`
- Ключ указан правильно: `GEMINI_API_KEY=AIzaSy...`
- Нет лишних пробелов вокруг значения

**Пример правильного `.env`:**
```env
BOT_TOKEN=7933882087:AAFoECTh0FPwt5j5KP-jTZI9izj6K8JIo60
GEMINI_API_KEY=AIzaSyYourKeyHere
NODE_ENV=development
```

### 2. Проверьте Gemini API ключ

1. Перейдите на https://aistudio.google.com/app/apikey
2. Проверьте что ключ активен и не истёк
3. При необходимости создайте новый ключ
4. Убедитесь что **включен доступ к Gemini API**

### 3. Перезапустите бота

```bash
# Остановите бота если он запущен (Ctrl+C в терминале)

# Перезапустите
cd /Users/user/Desktop/heal_bot
npm start
```

### 4. Тестирование

1. Отправьте `/start` боту
2. Запустите `/practice`
3. Напишите текст (например: "Сегодня хороший день")
4. **Смотрите на логи в терминале** - теперь будет видно детальную ошибку

### Что искать в логах

✅ **Успешный запрос:**
```
📤 Отправка запроса в Gemini...
✅ Получен ответ от Gemini
```

❌ **Ошибка API ключа:**
```
❌ Ошибка Gemini API:
Тип ошибки: GoogleGenerativeAIError
Сообщение: API key not valid
HTTP статус: 400
```

❌ **Ошибка модели:**
```
❌ Ошибка Gemini API:
Тип ошибки: GoogleGenerativeAIError
Сообщение: Model not found
```

## Возможные проблемы и решения

### Проблема 1: "API key not valid"
**Решение:** 
- Проверьте правильность ключа в `.env`
- Создайте новый ключ на https://aistudio.google.com/app/apikey
- Убедитесь что скопировали весь ключ целиком

### Проблема 2: "Model not found"
**Решение:** 
- ✅ Уже исправлено - переключились на `gemini-1.5-flash`
- Перезапустите бота

### Проблема 3: "Quota exceeded"
**Решение:**
- Бесплатный tier имеет лимиты (60 req/min, 1500 req/day)
- Подождите и попробуйте снова
- Убедитесь что не делаете слишком много запросов

### Проблема 4: ".env file not found"
**Решение:**
```bash
# Создайте файл .env
cd /Users/user/Desktop/heal_bot
touch .env

# Откройте в редакторе и добавьте:
nano .env

# Вставьте:
BOT_TOKEN=ваш_токен
GEMINI_API_KEY=ваш_ключ
NODE_ENV=development

# Сохраните: Ctrl+O, Enter, Ctrl+X
```

## После исправления

1. Остановите бота (Ctrl+C)
2. Запустите снова: `npm start`
3. Протестируйте практику
4. **Смотрите логи** - теперь будет понятно что именно не так

## Дополнительная помощь

Если ошибка повторяется:
1. Скопируйте **точное сообщение ошибки** из логов
2. Проверьте что интернет соединение стабильно
3. Попробуйте другой API ключ

---

**Важно:** Теперь бот использует стабильную модель `gemini-1.5-flash` вместо экспериментальной, это должно решить большинство проблем!
