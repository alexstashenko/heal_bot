import { GoogleGenerativeAI } from '@google/generative-ai';

/**
 * Сервис для работы с Gemini Flash API
 */
class GeminiService {
    constructor(apiKey) {
        this.genAI = new GoogleGenerativeAI(apiKey);
        this.model = this.genAI.getGenerativeModel({
            model: 'gemini-2.5-flash',
            generationConfig: {
                temperature: 0.7,
                maxOutputTokens: 1500, // Увеличено для thinking mode
            }
        });
    }

    /**
     * Анализирует конкретный шаг практики HEAL
     * @param {string} step - Шаг HEAL: 'H', 'E', 'A', 'L'
     * @param {string} userText - Текст от пользователя
     * @param {Object} previousContext - История предыдущих шагов
     * @returns {Promise<string>} - Анализ от Gemini
     */
    async analyzeHealStep(step, userText, previousContext = {}) {
        const stepNames = {
            H: 'Having (Осознай опыт)',
            E: 'Enriching (Обогати ощущение)',
            A: 'Absorbing (Впитай в себя)',
            L: 'Linking (Создай связь)'
        };

        // Формируем контекст из предыдущих шагов
        let contextText = '';
        const stepOrder = ['H', 'E', 'A', 'L'];
        const currentStepIndex = stepOrder.indexOf(step);

        for (let i = 0; i < currentStepIndex; i++) {
            const prevStep = stepOrder[i];
            if (previousContext[prevStep]) {
                contextText += `\\nШаг ${prevStep}: ${previousContext[prevStep].userText}\\n`;
            }
        }

        const stepPrompts = {
            H: `Шаг H (Having) - помогите пользователю осознать текущий опыт ОК-ности.

Пользователь описал:
"${userText}"

ВАЖНАЯ ПРОВЕРКА на дистресс:
Если пользователь описывает, что ему ПЛОХО, у него проблемы, боль, сильная тревога, паника, кризис - НЕ давайте обычный инсайт.
Вместо этого ответьте:

"Я слышу, что сейчас вам непросто. 💙

Важно позаботиться о себе:
• Если нужна срочная помощь: Телефон доверия 8-800-2000-122 (24/7, бесплатно)
• Если есть боль или недомогание - обратитесь к врачу
• Сделайте что-то маленькое для комфорта: выпейте воды, укройтесь теплым, выйдите на воздух

Практика ОК-ности работает лучше, когда базовые потребности в безопасности удовлетворены. Позаботьтесь о себе. 🤍"

Если пользователь в относительном порядке:
Отметьте телесные сигналы безопасности, которые он заметил.
Дайте короткое поддерживающее отражение (1-2 предложения).
Будьте успокаивающим и заземляющим.`,

            E: `Шаг E (Enriching) - помогите углубить и обогатить опыт ОК-ности.

Контекст предыдущего шага:${contextText}

Сейчас пользователь описал:
"${userText}"

Ваша задача:
Отметьте детали и нюансы, которые пользователь заметил при обогащении опыта.
Поддержите углубление внимания к приятным ощущениям.
1-2 предложения, тёплый тон.`,

            A: `Шаг A (Absorbing) - помогите интегрировать опыт ОК-ности.

Контекст предыдущих шагов:${contextText}

Сейчас пользователь описал:
"${userText}"

Ваша задача:
Поддержите процесс впитывания и интеграции этого состояния.
Отметьте важность того, что пользователь позволяет себе прочувствовать это.
1-2 предложения, заземляющий тон.`,

            L: `Шаг L (Linking) - помогите создать связь с ресурсным состоянием.

Полный контекст практики:${contextText}

Сейчас пользователь описал:
"${userText}"

Ваша задача:
Поддержите создание связи этого опыта ОК-ности с будущими ситуациями.
Отметьте важность того, что это состояние теперь доступно как ресурс.
1-2 предложения, воодушевляющий тон.`
        };

        const prompt = stepPrompts[step] + `\\n\\nВАЖНО:\\n- Это НЕ терапия, а практика самопомощи\\n- Не используйте клинические термины\\n- Отвечайте на русском языке\\n- Максимум 2-3 предложения\\n\\nОтвет:`;

        const maxRetries = 2;

        for (let attempt = 1; attempt <= maxRetries; attempt++) {
            try {
                console.log(`📤 HEAL Step ${step}: попытка ${attempt}/${maxRetries}`);
                const result = await this.model.generateContent(prompt);
                const response = await result.response;

                console.log('📋 Response details:', {
                    candidates: response.candidates?.length || 0,
                    finishReason: response.candidates?.[0]?.finishReason
                });

                if (!response.candidates || response.candidates.length === 0) {
                    if (attempt < maxRetries) {
                        await new Promise(resolve => setTimeout(resolve, 500));
                        continue;
                    }
                    return '';
                }

                const text = response.text();
                console.log(`✅ HEAL Step ${step} analysis complete`);

                if ((!text || text.trim().length === 0) && attempt < maxRetries) {
                    console.warn(`⚠️  Пустой ответ на шаге ${step}`);
                    await new Promise(resolve => setTimeout(resolve, 500));
                    continue;
                }

                return text.trim();
            } catch (error) {
                console.error(`❌ Ошибка на шаге ${step}:`, error.message);
                if (attempt === maxRetries) {
                    throw new Error('Не удалось получить ответ от AI. Попробуйте позже.');
                }
                await new Promise(resolve => setTimeout(resolve, 1000));
            }
        }

        return '';
    }

    /**
     * Отвечает на вопрос пользователя в контексте текущей практики HEAL
     */
    async answerHealQuestion(step, question, sessionContext) {
        const stepNames = {
            H: 'Having (Осознание опыта)',
            E: 'Enriching (Обогащение)',
            A: 'Absorbing (Впитывание)',
            L: 'Linking (Связывание)'
        };

        let contextText = '';
        for (const [s, data] of Object.entries(sessionContext)) {
            if (data.userText) {
                contextText += `\\nШаг ${s}: ${data.userText}\\n`;
            }
        }

        const prompt = `Ты помогаешь пользователю в практике HEAL (осознанность и внутренний покой).

Пользователь сейчас на шаге ${step} (${stepNames[step]}).

Контекст практики:${contextText}

Пользователь задал вопрос:
"${question}"

Ваша задача:
Ответьте на вопрос тепло и по существу, в контексте текущего шага практики.
Помогите продолжить практику, не уводя в сторону.
2-3 предложения максимум.

ВАЖНО:
- Это практика самопомощи, не терапия
- Будьте поддерживающим и практичным
- Отвечайте на русском языке

Ответ:`;

        try {
            console.log(`💬 Ответ на вопрос на шаге ${step}`);
            const result = await this.model.generateContent(prompt);
            const response = await result.response;
            const text = response.text();

            return text.trim();
        } catch (error) {
            console.error('❌ Ошибка при ответе на вопрос:', error.message);
            return 'Извините, не смог обработать вопрос. Давайте продолжим практику? 🤍';
        }
    }

    /**
     * Генерирует итоговое отражение всей практики HEAL
     */
    async generateFinalSummary(fullHistory) {
        let historyText = '';
        const steps = ['H', 'E', 'A', 'L'];

        for (const step of steps) {
            if (fullHistory[step]?.userText) {
                historyText += `\\nШаг ${step}: ${fullHistory[step].userText}\\n`;
            }
        }

        const prompt = `Создай итоговое отражение всей практики HEAL.

История практики пользователя:${historyText}

Ваша задача:
Отразите путь пользователя через все 4 шага: H (осознание) → E (обогащение) → A (впитывание) → L (связь).
Подчеркните ценность этого опыта и доступность этого ресурсного состояния.
2-3 предложения, тёплый и воодушевляющий тон.

ВАЖНО:
- Это завершение практики
- Будьте поддерживающим
- Отвечайте на русском языке

Ответ:`;

        try {
            console.log('🎊 Генерация итогового summary');
            const result = await this.model.generateContent(prompt);
            const response = await result.response;
            const text = response.text();

            return text.trim();
        } catch (error) {
            console.error('❌ Ошибка генерации summary:', error.message);
            return 'Прекрасная работа! Вы прошли через все 4 шага практики HEAL и создали для себя ресурс внутреннего покоя. 🌟';
        }
    }

    /**
     * Проверка работоспособности API с таймаутом
     */
    async healthCheck() {
        try {
            console.log('Тестовый запрос к Gemini API...');

            const timeoutPromise = new Promise((_, reject) =>
                setTimeout(() => reject(new Error('Timeout')), 10000)
            );

            const checkPromise = this.model.generateContent('Test');

            await Promise.race([checkPromise, timeoutPromise]);

            console.log('✅ Gemini API отвечает');
            return true;
        } catch (error) {
            console.error('⚠️  Gemini API недоступен:', error.message);
            if (error.status) {
                console.error('HTTP статус:', error.status);
            }
            return false;
        }
    }
}

export default GeminiService;
