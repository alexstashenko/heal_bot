import { GoogleGenerativeAI } from '@google/generative-ai';

/**
 * Сервис для работы с Gemini Flash API
 */
class GeminiService {
    constructor(apiKey) {
        this.genAI = new GoogleGenerativeAI(apiKey);
        this.model = this.genAI.getGenerativeModel({
            model: 'gemini-2.5-flash',
            generationConfig: {
                temperature: 0.7,
                maxOutputTokens: 1500, // Увеличено для thinking mode
            }
        });
    }

    /**
     * Анализирует практику ОК-ности пользователя
     * @param {string} userText - Текст от пользователя
     * @returns {Promise<string>} - Анализ от Gemini
     */
    async analyzeOkness(userText) {
        const maxRetries = 2;

        for (let attempt = 1; attempt <= maxRetries; attempt++) {
            try {
                const prompt = `Ты - помощник для wellness-практик, помогаешь людям развивать осознанность и внутренний покой через практику ОК-ности.

Пользователь описал свое состояние прямо сейчас:
"${userText}"

Практика ОК-ности - это осознание, что прямо сейчас с человеком все в порядке: тело функционирует, дыхание есть, окружение безопасно. Это помогает выключить ненужный тревожный фон и обрести покой.

ВАЖНАЯ ПРОВЕРКА:
Если пользователь описывает, что ему ПЛОХО, у него проблемы, боль, тревога, паника, кризис - НЕ давай обычный инсайт.
Вместо этого ответь:

"Я слышу, что сейчас тебе непросто. 💙

Важно позаботиться о себе:
• Если нужна срочная помощь: Телефон доверия 8-800-2000-122 (24/7, бесплатно)
• Если есть боль или недомогание - обратись к врачу
• Сделай что-то маленькое для комфорта: выпей воды, укройся теплым, выйди на воздух

Практика ОК-ности работает лучше, когда базовые потребности в безопасности удовлетворены. Позаботься о себе. 🤍"

Если пользователь описывает, что с ним ВСЕ В ПОРЯДКЕ (дышит нормально, тело функционирует, спокойно, безопасно):

Твоя задача:
1. Отметь телесные сигналы или ощущения безопасности, которые пользователь заметил
2. Подтверди и усиль осознание, что прямо сейчас все в порядке
3. Дай короткий инсайт (1-2 предложения) о важности этого момента покоя
4. Будь успокаивающим и заземляющим

ВАЖНО: 
- Это НЕ терапия, а практика самопомощи
- Не используй клинические термины (депрессия, тревожное расстройство и т.д.)
- Отвечай на русском языке
- Максимум 2-3 предложения (только если пользователь В ПОРЯДКЕ)

Ответ:`;

                console.log(`📤 Отправка запроса в Gemini (попытка ${attempt}/${maxRetries})...`);
                const result = await this.model.generateContent(prompt);
                const response = await result.response;

                // Детальное логирование response
                console.log('📋 Response details:', {
                    candidates: response.candidates?.length || 0,
                    promptFeedback: response.promptFeedback,
                    usageMetadata: response.usageMetadata
                });

                // Проверяем на блокировку safety filters
                if (response.promptFeedback?.blockReason) {
                    console.error('🚫 Контент заблокирован:', response.promptFeedback.blockReason);
                    throw new Error('Контент был заблокирован фильтрами безопасности');
                }

                // Проверяем есть ли candidates
                if (!response.candidates || response.candidates.length === 0) {
                    console.error('⚠️  Нет candidates в ответе');
                    if (attempt < maxRetries) {
                        await new Promise(resolve => setTimeout(resolve, 500));
                        continue;
                    }
                    return '';
                }

                // Проверяем finishReason
                const candidate = response.candidates[0];
                console.log('🔍 Candidate finishReason:', candidate.finishReason);

                if (candidate.finishReason === 'SAFETY') {
                    console.error('🚫 Ответ заблокирован safety filters');
                    console.error('Safety ratings:', candidate.safetyRatings);
                    throw new Error('Контент был заблокирован фильтрами безопасности');
                }

                const text = response.text();

                console.log('✅ Получен ответ от Gemini');

                // Если ответ пустой и есть ещё попытки - повторяем
                if ((!text || text.trim().length === 0) && attempt < maxRetries) {
                    console.warn(`⚠️  Пустой ответ, попытка ${attempt}/${maxRetries}`);
                    await new Promise(resolve => setTimeout(resolve, 500)); // Небольшая задержка
                    continue;
                }

                return text.trim();
            } catch (error) {
                console.error('❌ Ошибка Gemini API:');
                console.error('Тип ошибки:', error.name);
                console.error('Сообщение:', error.message);
                if (error.response) {
                    console.error('Детали ответа:', error.response);
                }
                if (error.status) {
                    console.error('HTTP статус:', error.status);
                }

                // Если это последняя попытка - пробрасываем ошибку
                if (attempt === maxRetries) {
                    throw new Error('Не удалось получить ответ от AI. Попробуйте позже.');
                }

                // Иначе ждём и повторяем
                console.log(`🔄 Повтор через 1 секунду...`);
                await new Promise(resolve => setTimeout(resolve, 1000));
            }
        }

        // Если дошли сюда - все попытки вернули пустой ответ
        return '';
    }

    /**
     * Проверка работоспособности API с таймаутом
     * @returns {Promise<boolean>}
     */
    async healthCheck() {
        try {
            console.log('Тестовый запрос к Gemini API...');

            // Таймаут 10 секунд
            const timeoutPromise = new Promise((_, reject) =>
                setTimeout(() => reject(new Error('Timeout')), 10000)
            );

            const checkPromise = this.model.generateContent('Test');

            await Promise.race([checkPromise, timeoutPromise]);

            console.log('✅ Gemini API отвечает');
            return true;
        } catch (error) {
            console.error('⚠️  Gemini API недоступен:', error.message);
            if (error.status) {
                console.error('HTTP статус:', error.status);
            }
            return false;
        }
    }
}

export default GeminiService;
