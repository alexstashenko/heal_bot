# 🚀 HEAL AI MVP - Quick Start Guide

## 📅 День 1: Регистрация и подготовка (2-3 часа)

### ✅ Чеклист утром

#### 1. Создание Telegram бота (15 минут)
```
1. Открыть Telegram
2. Найти @BotFather
3. Отправить /newbot
4. Выбрать имя: "HEAL Wellness Bot" (или свое)
5. Выбрать username: heal_wellness_bot (должен заканчиваться на _bot)
6. СОХРАНИТЬ токен в безопасное место (это BOT_TOKEN)
7. Настроить описание: /setdescription
8. Добавить команды: /setcommands
   start - Начать работу с ботом
   practice - Практика HEAL
   history - История записей
   help - Помощь
```

#### 2. Регистрация на Railway (10 минут)
```
1. Открыть railway.app
2. Sign Up через GitHub
3. Подтвердить email
4. Создать новый проект: "heal-ai-bot"
5. Пока не деплоить - вернемся к этому позже
```

#### 3. OpenAI или Anthropic API (10 минут)
```
Выбор:
- OpenAI (рекомендовано): platform.openai.com
  * GPT-4o-mini: $0.15/1M input tokens
  * Для MVP хватит $10-20/месяц
  
- Anthropic: console.anthropic.com
  * Claude 3.5 Haiku: $0.25/1M input tokens
  * Немного дороже, но отличное качество

Действия:
1. Зарегистрироваться
2. Добавить платежный метод ($5-10 на старт)
3. Создать API key
4. СОХРАНИТЬ key в безопасное место
```

#### 4. Установка инструментов (30 минут)

**Node.js:**
```bash
# Проверить версию
node --version

# Если нет или версия < 18:
# Windows: скачать с nodejs.org
# Mac: brew install node
# Linux: sudo apt install nodejs npm
```

**Git:**
```bash
git --version

# Если нет:
# Windows: git-scm.com
# Mac: уже есть или brew install git
# Linux: sudo apt install git
```

**AI IDE (выбрать один):**

**Вариант 1: Cursor (рекомендовано)**
```
1. Скачать: cursor.sh
2. Установить
3. При первом запуске: Sign up
4. Триал 14 дней бесплатно
5. После триала: $20/месяц (стоит того!)
```

**Вариант 2: Windsurf (бесплатный)**
```
1. Скачать: codeium.com/windsurf
2. Установить
3. Sign up
4. Полностью бесплатный
```

**Вариант 3: Cline (VSCode extension)**
```
1. Открыть VSCode
2. Extensions → искать "Cline"
3. Установить
4. Настроить с вашим OpenAI/Anthropic API key
5. Бесплатный, но нужен свой API key
```

### ✅ Чеклист вечером

#### 5. Создание проекта (1 час)

**Инициализация:**
```bash
# Создать директорию
mkdir heal-ai-bot
cd heal-ai-bot

# Git init
git init

# NPM init
npm init -y

# Установить зависимости
npm install telegraf express dotenv prisma @prisma/client openai node-cron bcrypt zod

# Dev зависимости
npm install -D nodemon prisma
```

**Структура файлов:**
```bash
mkdir -p src/{config,handlers,services,utils,middleware}
mkdir -p prisma
mkdir tests

# Создать основные файлы
touch src/bot.js
touch src/config/index.js
touch .env
touch .gitignore
touch README.md
```

**Файл .gitignore:**
```
node_modules/
.env
dist/
*.log
.DS_Store
```

**Файл .env:**
```
BOT_TOKEN=ваш_токен_от_BotFather
OPENAI_API_KEY=ваш_openai_key
DATABASE_URL=postgresql://user:password@localhost:5432/healbot
ENCRYPTION_KEY=сгенерируйте_32_байта_ключ
NODE_ENV=development
PORT=3000
```

**Генерация ENCRYPTION_KEY:**
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

#### 6. Первый контакт с клиентами (30 минут)

**Выберите 5 самых активных участников тренинга**

Критерии:
- Активно участвовали в тренинге
- Дали положительный feedback
- Готовы помочь с тестированием

**Сообщение в Telegram/WhatsApp:**
```
Привет [Имя]! 👋

Помнишь нашу практику HEAL на тренинге? 

Я создаю Telegram-бота, который поможет делать её каждый день - 
просто, быстро, с AI-подсказками 🧠

Планирую запустить закрытую альфа через 2-3 недели.

Хочешь быть среди первых тестировщиков? 
Дам бесплатный доступ ко всем функциям на месяц + 
навсегда останется специальная цена (299₽ вместо 599₽).

Что скажешь? 😊
```

**Цель**: 3-5 человек согласятся

---

## 📅 День 2: Первый код (3-4 часа)

### Используем AI IDE!

#### Промпт 1: Базовая структура

**Открыть AI IDE (Cursor/Windsurf) и сказать:**

```
Создай структуру Node.js проекта для Telegram-бота:

Структура:
heal-ai-bot/
├── src/
│   ├── bot.js (главный файл)
│   ├── config/
│   │   └── index.js
│   ├── handlers/
│   │   ├── start.js
│   │   └── help.js
│   └── services/
│       └── database.js
├── .env
├── .gitignore
├── package.json
└── README.md

Требования:
1. bot.js должен:
   - Импортировать Telegraf
   - Загружать BOT_TOKEN из .env
   - Обрабатывать /start и /help
   - Логировать все сообщения
   
2. handlers/start.js:
   - Приветствие
   - Inline keyboard с кнопками:
     * "🧠 Начать практику"
     * "📊 Статистика"
   
3. config/index.js:
   - Экспорт всех переменных из .env
   
Используй ES6 modules (type: "module").
Добавь комментарии на русском.
```

#### Промпт 2: Проверка работоспособности

```
Добавь в bot.js функционал для проверки:

1. При запуске показать:
   - "✅ Бот запущен"
   - Username бота
   - NODE_ENV
   
2. Логировать каждое сообщение:
   - User ID
   - Username
   - Текст сообщения
   - Timestamp

3. Graceful shutdown при Ctrl+C:
   - "🛑 Бот остановлен"
   - bot.stop()
```

#### Запуск бота локально

```bash
# Запустить
node src/bot.js

# Если все ОК, должно вывести:
# ✅ Бот запущен
# Username: @your_bot_username
# NODE_ENV: development
```

**Тест в Telegram:**
1. Найти бота по username
2. Отправить /start
3. Должно прийти приветствие с кнопками
4. Нажать кнопку - должен прийти ответ

**Если не работает:**
- Проверить BOT_TOKEN в .env
- Проверить логи в консоли
- Убедиться, что нет опечаток

---

## 📅 День 3: База данных (2-3 часа)

### Railway PostgreSQL

#### 1. Создать БД на Railway (15 минут)

```
1. Открыть railway.app
2. Ваш проект "heal-ai-bot"
3. New → Database → PostgreSQL
4. Дождаться создания
5. Скопировать DATABASE_URL из Variables
6. Вставить в .env
```

#### 2. Prisma Schema

**Промпт для AI IDE:**

```
Создай Prisma schema (prisma/schema.prisma) для wellness-приложения:

Таблицы:
1. User:
   - id (UUID)
   - telegramId (String, unique, indexed)
   - username (String, optional)
   - subscriptionTier ("FREE" | "PREMIUM")
   - consentGiven (Boolean, default false)
   - createdAt, updatedAt

2. Entry:
   - id (UUID)
   - userId (foreign key)
   - content (String - будет зашифрован)
   - healStep ("HAVE" | "ENRICH" | "ABSORB" | "LINK")
   - date (DateTime, indexed)
   - createdAt

3. UserStats:
   - id (UUID)
   - userId (foreign key, unique)
   - totalPractices (Int, default 0)
   - currentStreak (Int, default 0)
   - lastPracticeAt (DateTime, nullable)

Добавь:
- relations
- @@map для имен таблиц
- @@index для быстрого поиска

Используй PostgreSQL.
```

#### 3. Миграция

```bash
# Инициализация Prisma
npx prisma init

# Применить schema
npx prisma migrate dev --name init

# Открыть Prisma Studio (опционально)
npx prisma studio
```

#### 4. Database Service

**Промпт для AI IDE:**

```
Создай src/services/database.js с Prisma клиентом:

Функции:
1. createUser(telegramId, userData)
2. getUserByTelegramId(telegramId)
3. createEntry(userId, content, healStep)
4. getRecentEntries(userId, days = 7)

Используй:
- async/await
- try-catch для всех операций
- JSDoc комментарии
- Graceful shutdown

Экспорт singleton PrismaClient.
```

---

## 📅 День 4-7: Следуйте основному плану

См. документ "HEAL_AI_MVP_Plan_Solo_Developer.docx" для детального плана недель 1-4.

---

## 🔧 Troubleshooting

### Проблема: Бот не отвечает

**Решение:**
```bash
# Проверить логи
node src/bot.js

# Проверить токен
echo $BOT_TOKEN

# Проверить webhook
curl https://api.telegram.org/bot<YOUR_TOKEN>/getWebhookInfo

# Удалить webhook (если есть)
curl https://api.telegram.org/bot<YOUR_TOKEN>/deleteWebhook
```

### Проблема: Ошибки с Prisma

**Решение:**
```bash
# Переустановить Prisma
npm uninstall prisma @prisma/client
npm install -D prisma
npm install @prisma/client

# Пересоздать миграции
rm -rf prisma/migrations
npx prisma migrate dev --name init

# Сгенерировать клиент
npx prisma generate
```

### Проблема: AI IDE выдает странный код

**Решение:**
1. Будьте более конкретны в промпте
2. Разбейте задачу на меньшие части
3. Дайте примеры кода
4. Попросите AI объяснить код

### Проблема: OpenAI API ошибки

**Решение:**
```javascript
// Добавить retry логику
const retry = async (fn, retries = 3) => {
  for (let i = 0; i < retries; i++) {
    try {
      return await fn();
    } catch (error) {
      if (i === retries - 1) throw error;
      await new Promise(r => setTimeout(r, 1000 * (i + 1)));
    }
  }
};
```

---

## 📊 Метрики первых 7 дней

**День 1:**
- [ ] Бот создан
- [ ] Railway аккаунт
- [ ] OpenAI API key
- [ ] 5 клиентов уведомлены

**День 2:**
- [ ] Базовый бот работает локально
- [ ] Отвечает на /start и /help
- [ ] Есть inline кнопки

**День 3:**
- [ ] БД создана на Railway
- [ ] Prisma schema готов
- [ ] Миграции применены

**День 4-5:**
- [ ] HEAL flow реализован
- [ ] Записи сохраняются в БД
- [ ] Базовая статистика работает

**День 6-7:**
- [ ] Первый деплой на Railway
- [ ] 1-2 тестера начали использовать
- [ ] Собран первый feedback

---

## 💡 Советы для соло-разработчика

### Управление временем

**Правило 80/20:**
- 80% времени на core функционал (HEAL практика)
- 20% на polish и доп. фичи

**Батчинг задач:**
- Утро: сложные задачи (архитектура, новый код)
- День: интеграции (API, БД)
- Вечер: тестирование и feedback

**Перерывы:**
- Каждые 90 минут - 15 минут отдыха
- Используйте собственную HEAL практику!

### Работа с AI IDE

**Do:**
- ✅ Начинать с четких промптов
- ✅ Давать контекст проекта
- ✅ Итеративно уточнять
- ✅ Всегда проверять код

**Don't:**
- ❌ Копировать код без понимания
- ❌ Игнорировать ошибки
- ❌ Делать все одним промптом
- ❌ Забывать про security

### Feedback от клиентов

**Как собирать:**
- Короткие опросы в боте (1-2 вопроса)
- Звонки по 15 минут раз в неделю
- Telegram группа для бета-тестеров
- Google Forms для структурированного feedback

**Что спрашивать:**
- Что нравится больше всего?
- Что раздражает или непонятно?
- Используете ли каждый день?
- Порекомендуете ли друзьям?

---

## 🎯 Критерии успеха первой недели

**MVP считается успешным если:**
- [ ] Бот работает стабильно
- [ ] 3+ тестера используют ежедневно
- [ ] HEAL flow работает end-to-end
- [ ] Нет критических багов
- [ ] Положительный feedback от тестеров

**Если не выполнены критерии:**
- Не паникуйте!
- Соберите детальный feedback
- Приоритизируйте фиксы
- Итерируйте быстро (1-2 дня на фикс)

---

## 📞 Ресурсы и помощь

**Документация:**
- Telegraf.js: https://telegraf.js.org
- Prisma: https://prisma.io/docs
- OpenAI: https://platform.openai.com/docs
- Railway: https://docs.railway.app

**Сообщества:**
- r/nodejs (Reddit)
- Node.js Discord
- Telegram: @nodejs_ru

**AI помощники:**
- ChatGPT / Claude для вопросов
- Stack Overflow
- GitHub Issues конкретных библиотек

---

**Помните**: Первая неделя - это марафон, не спринт. Фокусируйтесь на запуске базовой версии, а не на идеальном продукте. Feedback от реальных пользователей ценнее внутренних предположений!

Удачи! 🚀
